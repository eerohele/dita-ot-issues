# ant.targets = build-init preprocess.init gen-list debug-filter mapref branch-filter copy-files keyref
args.input = ${user.dir}/subdir/root.ditamap
args.rellinks = all
generate.copy.outer = 3
transtype = pdf
